# Required: Google Gemini API key for the analysis agent
# Get one at https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-api-key-here

# Optional: Model override (default: google_genai:gemini-3-flash-preview)
# VA_MODEL_NAME=google_genai:gemini-3-flash-preview

# Optional: Temperature (default: 0.0 for deterministic)
# VA_TEMPERATURE=0.0

# Optional: Max SQL rows returned per query (default: 500)
# VA_MAX_ROWS=500

# Optional: Per-query timeout in seconds (default: 30)
# VA_QUERY_TIMEOUT=30

# Optional: Warehouse DB path (default: runs/warehouse.db)
# VA_DB_PATH=runs/warehouse.db
